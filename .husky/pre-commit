#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Temporarily disable pre-commit hook due to ESLint config loading issue
# The error "Exit prior to config file resolving cause call config.load() before reading values"
# appears to be a bug in ESLint 9.x with flat config loading
# 
# Workaround: Run lint manually before committing or use: git commit --no-verify
# 
# To re-enable, uncomment below once ESLint config loading is fixed:
# STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ts|tsx)$' || true)
# if [ -n "$STAGED_FILES" ]; then
#   ESLINT_USE_FLAT_CONFIG=1 npx eslint --fix --max-warnings 0 $STAGED_FILES
# fi
