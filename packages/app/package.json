{
  "name": "@hyperscape/app",
  "version": "0.13.0",
  "type": "module",
  "private": true,
  "description": "Hyperscape native desktop and mobile app powered by Tauri v2",
  "scripts": {
    "dev": "tauri dev",
    "build": "CI=true tauri build --no-bundle",
    "build:full": "CI=true tauri build",
    "build:debug": "CI=true tauri build --debug",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "ios:build": "CI=true tauri ios build",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "CI=true tauri android build",
    "icon": "tauri icon src-tauri/icons/app-icon.png",
    "tauri": "tauri"
  },
  "dependencies": {
    "@hyperscape/client": "workspace:*",
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-deep-link": "^2.2.0",
    "@tauri-apps/plugin-dialog": "^2.2.0",
    "@tauri-apps/plugin-http": "^2.2.0",
    "@tauri-apps/plugin-notification": "^2.2.0",
    "@tauri-apps/plugin-opener": "^2.3.0",
    "@tauri-apps/plugin-os": "^2.2.0",
    "@tauri-apps/plugin-process": "^2.2.0",
    "@tauri-apps/plugin-updater": "^2.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  },
  "engines": {
    "node": ">=22.11.0",
    "bun": ">=1.1.38"
  },
  "license": "GPL-3.0-only"
}
