# ===========================================
# HYPERSCAPE CLIENT ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env and fill in your values.
#
# IMPORTANT: After changing .env values, you MUST restart the dev server
# for changes to take effect. Vite only loads .env files on startup.
#
# For production deployment (Vercel/Netlify/Cloudflare Pages):
#   Set these variables in your platform's dashboard.
#   See README.md for detailed deployment instructions.
#
# Note: Only PUBLIC_ and VITE_ prefixed variables are exposed to the client
# (configured in vite.config.ts with envPrefix: 'PUBLIC_')

# ===========================================
# AUTHENTICATION (Required)
# ===========================================
# Get your App ID from https://dashboard.privy.io/
PUBLIC_PRIVY_APP_ID=your-privy-app-id-here

# ===========================================
# SERVER CONNECTION (Required for production)
# ===========================================
# In development, these default to localhost
# In production, set to your deployed server URLs

# Game server HTTP API
PUBLIC_API_URL=http://localhost:5555

# Game server WebSocket (for real-time multiplayer)
PUBLIC_WS_URL=ws://localhost:5555/ws

# CDN for game assets (models, textures, audio)
PUBLIC_CDN_URL=http://localhost:8080

# ===========================================
# AI DASHBOARD (Optional)
# ===========================================
# ElizaOS API for AI agent management dashboard
# PUBLIC_ELIZAOS_URL=http://localhost:4001

# ===========================================
# FARCASTER FRAME V2 (Optional)
# ===========================================
# Enable if deploying as a Farcaster mini-app
# PUBLIC_ENABLE_FARCASTER=true

# Public URL for your deployed app (required for Farcaster frames)
# PUBLIC_APP_URL=https://your-game.vercel.app

# ===========================================
# DEVELOPMENT (Optional)
# ===========================================
# Vite dev server port (default: 3333)
# VITE_PORT=3333

# Enable debug logging for RPG systems
# DEBUG_RPG=1

# ===========================================
# MOBILE / CAPACITOR (Optional)
# ===========================================
# For iOS/Android development with live reload
# Set to your local machine's IP address
# Find with: ipconfig getifaddr en0 (Mac) or hostname -I (Linux)
# CAP_SERVER_URL=http://192.168.1.100:3333
