# Nixpacks configuration for Railway deployment
# This file configures how Railway builds the Hyperscape server

[phases.setup]
# Install system dependencies needed for native modules (canvas, better-sqlite3)
aptPkgs = [
  "python3",
  "make",
  "g++",
  "git",
  "pkg-config",
  "libcairo2-dev",
  "libpango1.0-dev",
  "libjpeg-dev",
  "libgif-dev",
  "librsvg2-dev",
  "ca-certificates"
]

[phases.install]
# Use bun for package installation
cmds = ["bun install"]

[phases.assets]
# Clone game assets repository (contains manifests needed by DataManager)
dependsOn = ["install"]
cmds = ["git clone --depth 1 https://github.com/HyperscapeAI/assets.git packages/server/world/assets"]

[phases.build]
# Build the packages in correct order: shared -> client -> server
# Then copy client dist to server public for static serving
dependsOn = ["assets"]
cmds = [
  "bun run build:shared",
  "bun run build:client",
  "bun run build:server",
  "mkdir -p packages/server/public",
  "cp -r packages/client/dist/* packages/server/public/"
]

[start]
cmd = "bun dist/index.js"

[variables]
# Skip asset download in CI - assets are served from CDN
CI = "true"
SKIP_ASSETS = "true"
NODE_ENV = "production"
